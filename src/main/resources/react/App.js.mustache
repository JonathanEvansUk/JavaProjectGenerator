import { Container, Nav, Navbar } from "react-bootstrap";
import { NavLink, Route, Routes } from "react-router-dom";
import Entities from "./routes/entities";
import Home from "./routes/home";
{{#models}}import Create{{nameCapitalised}} from "./routes/{{name}}/Create{{nameCapitalised}}";
import {{nameCapitalised}}List from "./routes/{{name}}/{{nameCapitalised}}List";
import View{{nameCapitalised}} from "./routes/{{name}}/View{{nameCapitalised}}";
import Edit{{nameCapitalised}} from "./routes/{{name}}/Edit{{nameCapitalised}}";
{{/models}}

export const App = () => {
  return (
    <div>
      <Navbar bg="light">
        <Nav>
          <Nav.Link as={NavLink} to="/">
            Home
          </Nav.Link>
          <Nav.Link as={NavLink} to="/entities">
            Entities
          </Nav.Link>
        </Nav>
      </Navbar>

      <Container className="py-2">
        <Routes>
          <Route path="/" element={<Home />} />
          <Route path="entities" element={<Entities />} />
          {{#models}}
          <Route path="{{name}}" element={<Create{{nameCapitalised}} />} />
          <Route path="view{{nameCapitalised}}" element={<{{nameCapitalised}}List />} />
          <Route path="{{name}}/:id" element={<View{{nameCapitalised}} />} />
          <Route path="{{name}}/:id/edit" element={<Edit{{nameCapitalised}} />} />

          {{/models}}
        </Routes>
      </Container>
    </div>
  );
};